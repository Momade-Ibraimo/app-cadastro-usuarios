import React from 'react'
import { ContainerItens as ContainerI } from './styles'

function ContainerItens({children}) {
    return <ContainerI>{children}</ContainerI>
}

export default ContainerItens